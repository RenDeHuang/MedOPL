# 历史参考文档，不是 v22 active 合同或当前实现入口；不得作为当前主线、smoke、接云或部署依据。

# OPL v20.33 Update Log

Date: 2026-05-04

Target build tag: `opl-v20.33`

## Status

`pending-release-window`

This log records the v20.33 release checklist. It must be completed with real cloud evidence during the release window before v20.33 is marked ready for controlled commercial trial operation.

## Implemented Locally

- Portal shell fast path for `/portal/app/*`.
- Portal first-paint split loading for overview, billing, and resources.
- Bounded Portal read paths for runs and events.
- OPL launch preparation status page and status API.
- OPL gateway timing markers for launch ready, DOM ready, and first interaction.
- Beginner package flow with one recommended package entry.
- v20.33 production readiness script and preacceptance report template.

## Release Evidence To Fill

- Image tag: `opl-v20.33`
- Portal image digest:
- OPL gateway image digest:
- Runtime bridge image digest:
- Resource provisioner image digest:
- Billing aggregator image digest:
- Rendered manifest directory:
- Migration job name:
- Migration job result:
- Backup point:
- Rollout namespace:
- Portal deployment rollout history:
- OPL gateway deployment rollout history:
- Runtime bridge deployment rollout history:
- Rollback command:

## Readiness Evidence To Fill

- Production preacceptance evidence file:
- Isolated full loop evidence file:
- Production readiness output file:
- Cloud gate report:

## Commercial Decision

Do not mark v20.33 commercial-ready until:

- production preacceptance returns `ok=true`;
- isolated full loop returns `ok=true`;
- production readiness returns `status=ready`;
- delete and stop-billing evidence is present;
- rollback command and image digests are recorded.
