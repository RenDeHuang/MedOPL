# 历史参考文档，不是 v22 active 合同或当前实现入口；不得作为当前主线、smoke、接云或部署依据。

# v20.33商业化放量测试 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用可复跑证据证明 v20.33 是否可以从生产前候选进入受控商业化试运营，并给出是否允许继续放量的明确结论。

**Architecture:** 测试按 L0 到 L7 分层推进，每一层只在上一层证据通过后执行。只读入口和页面验收先行，真实创建资源、模型调用、删除资源、账单终验放在显式 opt-in 阶段，并要求留下 JSON evidence、命令输出、回滚命令和商业决策记录。

**Tech Stack:** Bash, Node.js smoke/live scripts, kubectl, gstack browse, Tencent TKE/CLB/COS billing evidence, Portal/OPL/Trace official hosts.

---

## 当前入口状态

记录时间：2026-05-05 00:21 Asia/Shanghai。

已复测到的入口事实：

- Google DNS 与 Cloudflare DNS 已把 `portal.medopl.cn`、`opl.medopl.cn`、`trace.medopl.cn` 解析到 `lb-7osxdayi-vodne4arkfxy067j.clb.usw-tencentclb.com`。
- 当前业务 CLB IP 为 `43.173.116.127`。
- `https://portal.medopl.cn/` 返回 302 到 `/login`，`/login` 返回 200。
- gstack browser 打开 `https://portal.medopl.cn/` 成功，页面文本包含 `统一门户登录` 和 `注册新账号`，console error 为 0。
- `https://opl.medopl.cn/` 返回 OPL HTML，`https://trace.medopl.cn/` 返回 Trace/Langfuse HTML。
- 当前云上运行态仍是 v20.32：
  - Portal build: `opl-v20.32-portal-runtime-wiring-20260504070952`
  - OPL gateway build: `opl-v20.32-gateway-message-bridge-20260504082220`

因此，入口恢复只证明 DNS/CLB 已经回到业务入口，不证明 v20.33 已经上线，也不证明 v20.33 可以商业化放量。

## 总则

1. 任何公开付费放量前，必须证明官方域名运行的是 `opl-v20.33`，不能用 v20.32 运行态替代。
2. `one-person-lab` upstream 不修改、不作为本计划写入范围。
3. Sentrux gate 当前不是硬性上线 blocker，但结构健康和 test gaps 结果必须记录到最终报告。
4. 真实云资源创建、模型调用、资源删除、账单观测只允许在显式 opt-in 环境变量齐全时执行。
5. 每一层失败后停止，不继续下一层；失败结论写入报告，不做临时绕过。
6. 商业化放量分三档决策：`not-approved`、`controlled-trial`、`paid-ramp`。没有 T+1 账单终验证据时，最高只能是 `controlled-trial`。

## 文件责任

- Read: `scripts/live-test-v20.33-production-preacceptance.mjs`
  只读生产前验收，验证登录、关键 API、Portal 页面、Portal 到 OPL 跳转、Trace 页面。
- Read: `scripts/live-test-v20.33-isolated-full-loop.mjs`
  显式 opt-in 完整闭环，创建测试用户、充值、下单、上传、创建资源、OPL 消息、文件任务、下载、trace、删除资源、观察停费。
- Read: `scripts/live-test-v20.33-portal-opl-message-loop.mjs`
  Portal 到 OPL 消息链路验证，覆盖 OPL 首次回复和完整回复延迟。
- Read: `scripts/check-v20.33-production-readiness.mjs`
  读取 full-loop evidence 与官方 host build，给出 `ready` 或 `failed`。
- Read: `scripts/check-one-person-lab-upstream-clean.mjs`
  证明 upstream 未被修改。
- Produce: `.runtime/v20.33-production-preacceptance/*.json`
  L3 只读生产前验收证据。
- Produce: `.runtime/v20.33-portal-opl-message-loop/*.json`
  L4 OPL 消息链路证据。
- Produce: `.runtime/v20.33-isolated-full-loop/*.json`
  L5 完整商业闭环证据。
- Produce: `docs/reports/2026-05-05-OPL-v20.33-Commercial-Ramp-Test-Report.md`
  最终测试报告，记录每层结论、证据路径、阻断项和放量决策。

## L0：本地不可变合同与构建

**目的：** 证明 v20.33 本地实现、前端构建、测试脚本合同和 upstream 边界是干净的。

**允许风险：** 无云资源、无模型调用、无生产写入。

- [ ] **Step 1：确认分支和工作区**

```bash
pwd
git status --short --branch
```

Expected:

- 当前目录是 `/home/dev/projects/platform-v20/platform-v20.33`
- 当前分支是 `codex/platform-v20.33`
- 没有未解释的工作区改动

- [ ] **Step 2：运行本地合同测试**

```bash
node scripts/check-one-person-lab-upstream-clean.mjs
node scripts/smoke-test-v20.33-evidence-contract.mjs
node scripts/smoke-test-v20.33-portal-shell-fast-path-contract.mjs
node scripts/smoke-test-v20.33-portal-rendering-contract.mjs
node scripts/smoke-test-v20.33-portal-read-path-contract.mjs
node scripts/smoke-test-v20.33-opl-launch-status-contract.mjs
node scripts/smoke-test-v20.33-opl-gateway-interaction-contract.mjs
node scripts/smoke-test-v20.33-beginner-package-flow-contract.mjs
node scripts/smoke-test-v20.33-portal-opl-message-loop-contract.mjs
node scripts/smoke-test-v20.33-isolated-full-loop-contract.mjs
node scripts/smoke-test-v20.33-production-preacceptance-contract.mjs
node scripts/smoke-test-v20.33-production-readiness-contract.mjs
```

Expected:

- 每条命令退出码为 0
- `check-one-person-lab-upstream-clean` 输出 `ok: true`

- [ ] **Step 3：运行 Portal 与前端构建**

```bash
npm --prefix services/portal run check
npm --prefix services/portal/frontend run typecheck
npm --prefix services/portal/frontend run build
```

Expected:

- 每条命令退出码为 0
- frontend build 成功生成生产构建

- [ ] **Step 4：记录 L0 结论**

把命令、退出码、失败日志路径写入 `docs/reports/2026-05-05-OPL-v20.33-Commercial-Ramp-Test-Report.md`。如果任一命令失败，最终决策为 `not-approved`，停止 L1。

## L1：正式入口 DNS、TLS、HTTP/HTTPS 与浏览器登录页

**目的：** 证明用户访问的正式入口已到业务 CLB，不再落到 Kubernetes API Server。

**允许风险：** 只读公网访问。

- [ ] **Step 1：核对公共 DNS**

```bash
for host in portal.medopl.cn opl.medopl.cn trace.medopl.cn; do
  echo "DNS_GOOGLE $host"
  curl -fsS "https://dns.google/resolve?name=${host}&type=CNAME"
  curl -fsS "https://dns.google/resolve?name=${host}&type=A"
  echo "DNS_CLOUDFLARE $host"
  curl -fsS -H 'accept: application/dns-json' "https://cloudflare-dns.com/dns-query?name=${host}&type=CNAME"
  curl -fsS -H 'accept: application/dns-json' "https://cloudflare-dns.com/dns-query?name=${host}&type=A"
done
```

Expected:

- 三个 host 的 CNAME 都是 `lb-7osxdayi-vodne4arkfxy067j.clb.usw-tencentclb.com`
- A 记录解析到 `43.173.116.127`
- 不出现 `lb-952pntps-mahtufc86zw9ksjo.clb.usw-tencentclb.com`

- [ ] **Step 2：核对 HTTPS 入口**

```bash
for url in \
  https://portal.medopl.cn/ \
  https://portal.medopl.cn/login \
  https://portal.medopl.cn/healthz \
  https://portal.medopl.cn/version \
  https://opl.medopl.cn/ \
  https://opl.medopl.cn/healthz \
  https://opl.medopl.cn/portal-adapter/healthz \
  https://trace.medopl.cn/ \
; do
  echo "URL $url"
  curl -sS -L -o /tmp/v20-33-entry-body -D /tmp/v20-33-entry-headers \
    -w 'url=%{url_effective} status=%{http_code} redirects=%{num_redirects} type=%{content_type} ip=%{remote_ip} ssl=%{ssl_verify_result} size=%{size_download} time=%{time_total}\n' \
    "$url"
  sed -n '1,8p' /tmp/v20-33-entry-body
done
```

Expected:

- `ssl=0`
- `portal.medopl.cn/` 最终到 `/login`，状态 200
- `portal.medopl.cn/healthz` 返回 JSON，`service` 为 `portal`
- `portal.medopl.cn/version` 不能返回 Kubernetes `gitVersion`
- `opl.medopl.cn/healthz` 返回 JSON，`service` 为 `opl-web-gateway`
- `trace.medopl.cn/` 返回 HTML，HTTP status 小于 500

- [ ] **Step 3：核对 HTTP 明文入口**

```bash
for url in \
  http://portal.medopl.cn/ \
  http://opl.medopl.cn/ \
  http://trace.medopl.cn/ \
; do
  echo "URL $url"
  curl -sS -L -o /tmp/v20-33-http-body -D /tmp/v20-33-http-headers \
    -w 'url=%{url_effective} status=%{http_code} redirects=%{num_redirects} type=%{content_type} ip=%{remote_ip} size=%{size_download} time=%{time_total}\n' \
    "$url"
  sed -n '1,12p' /tmp/v20-33-http-headers
done
```

Expected:

- `portal`、`opl`、`trace` 都不能落到 `43.173.117.203`
- 推荐结果是 HTTP 301/302 到 HTTPS
- 如果 OPL 或 Trace 直接 HTTP 200，记录为 P1 上线硬化项；不阻断小流量内测，但阻断公开付费放量

- [ ] **Step 4：浏览器打开 Portal 登录页**

```bash
B=/home/dev/.codex/skills/gstack/browse/dist/browse
$B restart
$B goto https://portal.medopl.cn/
$B text
$B console --errors
$B network
```

Expected:

- 页面文本包含 `统一门户登录`
- 页面文本包含 `注册新账号`
- console errors 为 `(no console errors)`
- network 中 `https://portal.medopl.cn/` 为 302，`https://portal.medopl.cn/login` 为 200

- [ ] **Step 5：记录 L1 结论**

如果 HTTPS 证书错误、DNS 指向旧 CLB、`/version` 返回 Kubernetes 版本、Portal 登录页打不开，最终决策为 `not-approved`，停止 L2。

## L2：v20.33 云上运行态确认

**目的：** 证明当前生产入口运行的是 v20.33，而不是 v20.32 或更旧版本。

**允许风险：** 只读 kubectl 和只读 HTTP。

- [ ] **Step 1：确认 kubeconfig 可读**

```bash
KUBECONFIG=/home/dev/.secrets/medopl/kubeconfig-v20.32-external kubectl get ns
```

Expected:

- 能列出 `default`、`portal-v20-32-staging`、`portal-runtime-v20-32-staging` 等 namespace
- 如果已有 v20.33 namespace，必须记录 namespace 名称和用途

- [ ] **Step 2：确认官方 Ingress 指向业务 CLB**

```bash
KUBECONFIG=/home/dev/.secrets/medopl/kubeconfig-v20.32-external \
kubectl describe ingress -n default gaofenglab
```

Expected:

- Address 是 `lb-7osxdayi-vodne4arkfxy067j.clb.usw-tencentclb.com`
- hosts 包含 `portal.medopl.cn`、`opl.medopl.cn`、`trace.medopl.cn`
- backend 指向业务 service，不指向 `kubernetes-extranet`

- [ ] **Step 3：确认部署镜像和 ready replicas**

```bash
KUBECONFIG=/home/dev/.secrets/medopl/kubeconfig-v20.32-external \
kubectl get deploy -A -o wide | sed -n '1,220p'

KUBECONFIG=/home/dev/.secrets/medopl/kubeconfig-v20.32-external \
kubectl get pods -A -o wide | sed -n '1,260p'
```

Expected:

- 面向正式入口的 Portal、OPL gateway、OPL adapter、resource provisioner、billing aggregator、runner orchestrator 镜像 tag 必须是 `opl-v20.33` 或带 v20.33 release suffix
- ready replicas 必须等于 desired replicas
- 不允许 crash loop

- [ ] **Step 4：确认应用 build tag**

```bash
curl -fsS https://portal.medopl.cn/healthz
curl -fsS https://opl.medopl.cn/healthz
curl -fsS https://opl.medopl.cn/portal-adapter/healthz
```

Expected:

- Portal build tag 等于 `opl-v20.33`
- OPL gateway build tag 等于 `opl-v20.33`
- OPL adapter build tag 等于 `opl-v20.33`

- [ ] **Step 5：记录 L2 结论**

如果官方 host 没有证明 `opl-v20.33`，最终决策为 `not-approved`。当前 2026-05-05 复测仍是 v20.32，因此在 v20.33 部署前不得继续执行 L3 到 L7 作为 v20.33 商业化证据。

## L3：生产前只读验收

**目的：** 用真实账号验证 v20.33 官方入口的登录、关键 API、关键页面、Portal 到 OPL 跳转、Trace 页面。

**允许风险：** 只读登录与页面访问，不创建 CVM，不发模型消息，不删除资源。

- [ ] **Step 1：加载生产验收环境**

```bash
test -f /home/dev/.secrets/medopl/v20.33-live.env
set -a
. /home/dev/.secrets/medopl/v20.33-live.env
set +a
```

Expected:

- env 文件存在
- `PORTAL_LIVE_EMAIL` 或 `PORTAL_ADMIN_EMAIL` 已设置
- `PORTAL_LIVE_PASSWORD` 或 `PORTAL_ADMIN_PASSWORD` 已设置

- [ ] **Step 2：运行 preacceptance**

```bash
RUN_V20_33_PREACCEPTANCE=1 \
V20_33_PORTAL_BASE_URL=https://portal.medopl.cn \
V20_33_OPL_BASE_URL=https://opl.medopl.cn \
V20_33_TRACE_BASE_URL=https://trace.medopl.cn \
V20_33_EXPECTED_BUILD_TAG=opl-v20.33 \
node scripts/live-test-v20.33-production-preacceptance.mjs
```

Expected:

- 输出 JSON 中 `ok=true`
- `status` 为 `live`
- evidence 写入 `.runtime/v20.33-production-preacceptance/`
- 所有 user pages 和 admin pages 的 API status 都是 2xx
- 页面 requiredText 均找到
- `portalOplJump.ok=true`
- `traceBusiness.ok=true`

- [ ] **Step 3：记录体验指标**

```bash
LATEST_PREACCEPTANCE="$(ls -t .runtime/v20.33-production-preacceptance/*.json | head -1)"
node -e 'const fs=require("fs"); const p=process.argv[1]; const j=JSON.parse(fs.readFileSync(p,"utf8")); console.log(JSON.stringify(j.evidence.experience,null,2));' "$LATEST_PREACCEPTANCE"
```

Expected:

- `maxPageLatencyMs`、`maxApiLatencyMs`、`portalToOplJumpLatencyMs`、`tracePageLatencyMs` 有数值
- 面向小白用户的目标：Portal 关键页面不超过 5 秒，Portal 到 OPL 跳转不超过 15 秒
- 超过目标时记录为 P0/P1 体验阻断项，不能直接进入 paid ramp

## L4：Portal 到 OPL 消息链路

**目的：** 证明用户可以从 Portal 进入 OPL 并拿到模型回复，不只是页面能打开。

**允许风险：** 真实模型调用，产生少量模型费用。

- [ ] **Step 1：加载消息链路账号**

```bash
test -f /home/dev/.secrets/medopl/v20.33-live.env
set -a
. /home/dev/.secrets/medopl/v20.33-live.env
set +a
```

Expected:

- `PORTAL_LIVE_EMAIL`、`PORTAL_LIVE_PASSWORD`、`OPL_LIVE_EMAIL`、`OPL_LIVE_PASSWORD`、`OPL_LIVE_GFLABTOKEN` 均已设置

- [ ] **Step 2：运行 OPL 消息 live gate**

```bash
RUN_V20_33_LIVE=1 \
V20_33_ISOLATED_TRANSPORT_CONFIRMED=1 \
V20_33_CONNECT_HOST=lb-7osxdayi-vodne4arkfxy067j.clb.usw-tencentclb.com \
V20_33_CONNECT_PORT=443 \
V20_33_PORTAL_BASE_URL=https://portal.medopl.cn \
V20_33_OPL_BASE_URL=https://opl.medopl.cn \
V20_33_EXPECTED_BUILD_TAG=opl-v20.33 \
node scripts/live-test-v20.33-portal-opl-message-loop.mjs
```

Expected:

- 输出 JSON 中 `ok=true`
- evidence 写入 `.runtime/v20.33-portal-opl-message-loop/`
- 包含首次回复延迟和完整回复延迟
- OPL 回复非空

## L5：单用户完整商业闭环

**目的：** 用一个自动生成测试用户证明小白商业闭环：注册/充值、买套餐、开资源、进 OPL、发消息、文件任务、下载、trace、删除资源、停费。

**允许风险：** 创建真实云资源、模型调用、删除真实云资源、产生小额云费用。必须在可监控时间窗执行。

- [ ] **Step 1：确认执行窗口**

执行前人工确认：

- 当前是低流量窗口
- TKE/CLB/COS/Tencent billing 控制台可访问
- 已有人盯资源残留和账单
- 已记录 rollback command
- 已确认 `V20_33_CONFIRM_DELETE=1`

- [ ] **Step 2：运行 full loop**

```bash
test -f /home/dev/.secrets/medopl/v20.33-live.env
set -a
. /home/dev/.secrets/medopl/v20.33-live.env
set +a

RUN_V20_33_FULL_LOOP=1 \
V20_33_ISOLATED_TRANSPORT_CONFIRMED=1 \
V20_33_CONNECT_HOST=lb-7osxdayi-vodne4arkfxy067j.clb.usw-tencentclb.com \
V20_33_CONNECT_PORT=443 \
V20_33_CONFIRM_DELETE=1 \
V20_33_PORTAL_BASE_URL=https://portal.medopl.cn \
V20_33_OPL_BASE_URL=https://opl.medopl.cn \
V20_33_TRACE_BASE_URL=https://trace.medopl.cn \
V20_33_EXPECTED_BUILD_TAG=opl-v20.33 \
V20_33_RECHARGE_AMOUNT=1000 \
V20_33_STORAGE_GB=10 \
V20_33_SERVER_PLAN_ID=cpu-2c4g \
V20_33_BILLING_OBSERVE_MINUTES=10 \
node scripts/live-test-v20.33-isolated-full-loop.mjs
```

Expected:

- 输出 JSON 中 `ok=true`
- `evidence.status=done`
- 必须包含这些 stage 且 `ok` 不为 false：
  - `admin_user_create`
  - `wallet_topup`
  - `portal_login`
  - `storage_order`
  - `workspace_upload`
  - `resource_quote`
  - `resource_freeze`
  - `resource_provision`
  - `user_resources`
  - `opl_message`
  - `opl_file_run`
  - `workspace_download`
  - `billing_trace`
  - `delete_node_pool`
  - `post_delete_resource_binding`
  - `billing_stop_observe`
  - `t0_120min_checkpoint`
- `resourceOrderId` 非空
- `billingTags` 包含 `tenantid`、`workspaceid`、`resourceorderid`、`runid`、`serverplanid`
- `workspace_download.sizeBytes > 0`
- `billing_trace.traceCount > 0`
- `post_delete_resource_binding.storageBillingStoppedAt` 非空

- [ ] **Step 3：运行 production readiness**

```bash
LATEST_FULL_LOOP="$(ls -t .runtime/v20.33-isolated-full-loop/*.json | head -1)"

RUN_V20_33_PRODUCTION_READINESS=1 \
V20_33_FULL_LOOP_EVIDENCE_FILE="$LATEST_FULL_LOOP" \
V20_33_PRODUCTION_HOSTS_CONFIRMED=1 \
V20_33_ROLLBACK_PLAN_CONFIRMED=1 \
V20_33_EXPECTED_BUILD_TAG=opl-v20.33 \
node scripts/check-v20.33-production-readiness.mjs
```

Expected:

- 输出 JSON 中 `ok=true`
- `status=ready`
- `hosts.portal.buildTag=opl-v20.33`
- `fullLoop.stageCount` 覆盖完整闭环

## L6：T+0/T+1 账单与残留终验

**目的：** 证明资源删除后没有继续计费、没有资源残留，并且 T+1 账单可以归因到 L5 同一条 `tenant/workspace/resourceOrder/run/serverPlan` 链。

**允许风险：** 只读账单和只读云资源核查；如果发现残留，停止放量并走正式清理流程。

- [ ] **Step 1：T+0 资源残留核查**

```bash
LATEST_FULL_LOOP="$(ls -t .runtime/v20.33-isolated-full-loop/*.json | head -1)"
node -e 'const fs=require("fs"); const p=process.argv[1]; const j=JSON.parse(fs.readFileSync(p,"utf8")); const stages=j.evidence.stages; const r=stages.find(s=>s.stage==="user_resources")||{}; console.log(JSON.stringify({resourceOrderId:r.resourceOrderId,nodePoolId:r.nodePoolId,cvmCount:r.cvmCount,billingTags:r.billingTags},null,2));' "$LATEST_FULL_LOOP"

KUBECONFIG=/home/dev/.secrets/medopl/kubeconfig-v20.32-external \
kubectl get pods,jobs,pvc -A -o wide | sed -n '1,260p'
```

Expected:

- Kubernetes 中没有与 L5 fixture `workspaceId`、`runId`、`resourceOrderId` 对应的残留 pod/job/pvc
- 云资源控制台或 TKE 查询中没有匹配的残留 node pool、CVM、ASG

- [ ] **Step 2：T+0 停费状态核查**

```bash
LATEST_FULL_LOOP="$(ls -t .runtime/v20.33-isolated-full-loop/*.json | head -1)"
node -e 'const fs=require("fs"); const p=process.argv[1]; const j=JSON.parse(fs.readFileSync(p,"utf8")); const stages=j.evidence.stages; console.log(JSON.stringify(stages.filter(s=>["post_delete_resource_binding","billing_stop_observe","t0_120min_checkpoint"].includes(s.stage)),null,2));' "$LATEST_FULL_LOOP"
```

Expected:

- `storageBillingStoppedAt` 非空
- `billing_stop_observe` 有 before/after 观测
- `t0_120min_checkpoint.expected120minMinutes=120`

- [ ] **Step 3：T+1 exact attribution**

在腾讯云 COS 日账单投递完成后，AI 必须使用 v20.33 专用证据记录完成 exact attribution。最低要求：

- 账单行能唯一匹配 L5 的 `tenantid`、`workspaceid`、`resourceorderid`、`runid`、`serverplanid`
- exact charge、refund 或 makeup charge 写入 ledger
- 重放同一账单不产生二次扣费
- 管理员客户账务、用户账单、resource order exact cost 三处一致

如果仓库尚无 `scripts/live-test-v20.33-cos-exact-reconcile.mjs`，AI 必须先补齐 v20.33 专用 exact billing gate，再执行 T+1 终验；不能用旧版本脚本的文件名和 evidence schema 冒充 v20.33 终验证据。

## L7：小白试点与放量阶梯

**目的：** 从机器证据进入真实小白体验验证，避免只证明 API 可用但用户不会用、卡住或误解账单。

**允许风险：** 真实试点用户、真实费用；必须设置人数、预算和停止条件。

- [ ] **Step 1：3 人内部小白试点**

执行范围：

- 3 个从未用过系统的人
- 每人只使用 `portal.medopl.cn`
- 每人完成注册/登录、选择推荐套餐、创建 OPL 实验室、发送一条消息、上传一个文件任务、查看账单、删除资源

通过标准：

- 3 人全部完成闭环
- 不需要工程师口头指导才能理解下一步
- 每人首个 OPL 可交互时间不超过 60 秒
- 没有人误以为删除后还会继续计费
- 账单页面能解释本次费用

- [ ] **Step 2：5 到 10 人受控付费试运营**

进入条件：

- L0 到 L6 全部通过
- 3 人小白试点通过
- rollback command、镜像 digest、rollout history、backup point 已写入报告

执行范围：

- 最多 10 个真实用户
- 每人预算上限明确
- 每日人工复核账单、资源残留和异常日志

停止条件：

- 任一用户无法登录
- 任一用户资源删除后仍有残留计费风险
- 任一用户账单无法解释
- OPL 首次可交互超过 120 秒并且复现
- 5xx 或 console error 影响核心路径

- [ ] **Step 3：25 到 50 人 paid ramp**

进入条件：

- 5 到 10 人受控付费试运营连续 3 天无 P0
- T+1 exact attribution 连续覆盖所有付费 run
- 每日残留检查为 0
- 客服/退款/异常账务流程可执行

通过标准：

- 付费用户成功率大于 95%
- 删除停费证据 100%
- exact attribution 覆盖率 100%
- P0 incident 为 0
- P1 incident 有明确 owner 和修复截止时间

## 最终决策模板

AI 完成测试后，在 `docs/reports/2026-05-05-OPL-v20.33-Commercial-Ramp-Test-Report.md` 写入以下结构：

```markdown
# OPL v20.33 Commercial Ramp Test Report

Date:
Branch:
Commit:
Decision: not-approved | controlled-trial | paid-ramp

## Evidence

- L0 local gates:
- L1 entry gates:
- L2 cloud runtime:
- L3 preacceptance:
- L4 message loop:
- L5 full loop:
- L6 billing and cleanup:
- L7 pilot:

## Blocking Issues

- P0:
- P1:

## Commercial Boundary

Allowed:
Not allowed:
Next review time:
```

决策规则：

- `not-approved`：L0-L5 任一层失败，或官方 host 不是 `opl-v20.33`。
- `controlled-trial`：L0-L5 通过，L6 T+1 exact attribution 未完成，但删除停费和残留检查通过；最多 3 到 5 个内部/熟人试点用户。
- `paid-ramp`：L0-L7 全部通过，T+1 exact attribution 通过，才允许 25 到 50 人付费放量。

## 自检

- Spec coverage：覆盖入口恢复复测、v20.33 运行态确认、只读验收、消息链路、完整商业闭环、账单终验、小白试点、放量决策。
- Placeholder scan：本计划没有使用未定义内容作为通过标准；动态文件通过 `ls -t` 命令解析。
- Type consistency：所有脚本名、环境变量、evidence 目录与当前仓库 v20.33 脚本一致；T+1 exact billing 明确要求 v20.33 专用 gate，不能复用旧版本证据冒充。
