# managed-user-loop-smoke-rename Design

The current user loop is a managed OPL SaaS loop, not an MVP owner surface. This package keeps the behavior and replaces active names only. Archived MVP provenance is intentionally not rewritten.
